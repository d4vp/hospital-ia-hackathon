Place `DateBaseHIS.xlsx` here **only on your local machine or server**.
This folder is git-ignored and docker-ignored: hospital data must never be
committed to the repository nor baked into a Docker image. In Docker it is
mounted as a volume (see `docker-compose.yml`).
